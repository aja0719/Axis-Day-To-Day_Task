************************** Day 10 *****************************
Unchecked Exceptions=>

   NullPoint Exception-
        Invoking a method from a null object.
        Accessing or modifying a null object’s field.
        Taking the length of null, as if it were an array.
        Accessing or modifying the slots of null object, as if it were an array.
        Throwing null, as if it were a Throwable value.
         When you try to synchronize over a null object.

   Arithmetic Exceptions-
         Mistake in Arithmetic logic e.g. 9/0
         
   indexOutOfBound Exception-
         if try to access elements of an arrya of string out of its range

   NumberFormat Exceptions-
         Type mismatch Exceptions

   Collections Exception-
       Database disconnection

   File IO Exception-
       Buffer Exception

       try-catch block=>
    All ur logic must be included ur try block-try block can have multiple logic
    All Exceptions Handling included in catch block-

    Syntax:

        try{
            //logic1
            //logic2
        } catch (typeExceptions e){
            System.out.println(e.getMessage());
        }catch (typeExceptions e){
            System.out.println(e.getMessage());
        }

-----------------------------------------------------------------------
Exceptions Propogation =>
     -Flow of Exception  inside your function or inside a class will call Exception Propogation.
     -top to bottom approach
 
 e.g.
     
     class name{
        method(){
            Exception;
        }
        method2(){
            m();Exception
        }
        main(){
            method2();Exception
        }
     }
-------------------------------------------------------------------------------------

throw=>
    throw keyword is used for thorw an Exception that occurs.

throws => 
    The "throws" keyword is used to declare exceptions. 
    It specifies that there may occur an exception in the method.
    It doesn't throw an exception. It is always used with method signature.
